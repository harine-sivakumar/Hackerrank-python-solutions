# Enter your code here. Read input from STDIN. Print output to STDOUT
# Read input from STDIN
n = input() # Number of elements (not strictly needed in the logic below)
numbers = list(map(int, input().split())) # List of integers

# Check both conditions and print the result
print(all(x > 0 for x in numbers) and any(str(x) == str(x)[::-1] for x in numbers))
